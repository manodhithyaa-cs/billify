<!-- Top bar -->
    <header
      class="bg-[#3a3945] rounded-2xl flex justify-between items-center px-6 py-3 shadow-[0_0_20px_rgba(0,0,0,0.3)]"
    >
      <div class="text-white font-semibold text-lg select-none" style="text-transform: capitalize;">/{{ page_name }}</div>
      <div class="flex items-center gap-4">
        <button
          class="bg-pink-600 text-white font-semibold rounded-md px-4 py-2 flex items-center gap-2 select-none"
        >
          <i class="fas fa-envelope"></i> SEND MESSAGES
        </button>
        <button
          class="bg-pink-600 text-white rounded-md p-2 select-none"
          aria-label="User profile"
        >
          <i class="fas fa-user"></i>
        </button>
      </div>
    </header>